stages:
  test:
    cmd: echo "Hello testing"

  train_data:
    cmd: |
      dvc pull
      python train.py
    deps:
    - train.py
    outs:
    - Dataset.csv
    metrics:
    - metrics.json:
        cache: false
